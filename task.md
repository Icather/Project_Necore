# 核心攻坚与远景待办列表 (TODOs)

## 阶段一：上下文管理与缓存特化 (KV Cache Optimization)
- `[ ]` **1. 编写测试插件**: 编写首个真正的 `deepseek-v4-pro-20260504.json` 插件，实测动态能力感知机制。
- `[ ]` **2. 思考链 (Reasoning) 隔离**: 修改请求拦截器，在发起多轮对话请求时，自动剔除历史记录中包含的 `reasoning_content`，避免破坏大模型的 API 规范及浪费 Token。
- `[ ]` **3. 阶梯式上下文截断 (Sliding Window)**: 摒弃逐句删除历史记录的做法，改为设定软硬上限，当达到硬上限时，一次性砍掉前端 30%~50% 的记忆，最大化保留长序列前缀以白嫖大模型硬盘缓存 (KV Cache)。

## 阶段二：灵魂引擎 (Soul Engine) 架构搭建
- `[ ]` **1. Identity (人格特化)**: 剥离固化的 System Prompt，支持多重人格配置文件加载，并联动 Compose 全局 UI 主题。
- `[ ]` **2. User (用户画像)**: 接入 Tool Calls 特性，赋予模型读写本地 `User.json` 档案的能力，实现长期记忆提取。
- `[ ]` **3. Soul (情绪状态机)**: 设计情感变量引擎，根据对话内容动态影响模型的心情指数 (Mood) 与好感度 (Affection)。
- `[ ]` **4. Heartbeat (自主唤醒机制)**: 基于 Android `WorkManager`，实现 AI 的后台静默唤醒与主动消息推送，由“工具”进化为“陪伴体”。
